import { Route } from '@angular/router';
import { BestBallComponent } from './best-ball/best-ball.component';

export const bestBallRoutes: Route[] = [
  { path: '', component: BestBallComponent },
];
